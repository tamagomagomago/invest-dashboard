name: Update investment dashboard

on:
  workflow_dispatch:

# Auto updates are disabled to stop GitHub Actions storage growth.
# Re-enable intentionally when the dashboard update volume is reduced.

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Auto update disabled
        run: echo "Investment dashboard auto update is disabled to reduce GitHub Actions storage usage."
